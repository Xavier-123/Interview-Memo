# Interview Memo · UI / 视觉设计优化建议报告

> 走查方式:浏览器实际渲染走查(1440×900 桌面视口 + 390×844 移动端结构验证,亮/暗双主题)+ 源码比对。
> 走查范围:总览、岗位(看板/表格)、面试列表、面试详情、岗位详情、模拟面试、日历、复盘、题库、分析、公司、简历、设置、新增岗位弹窗,共 16 个界面。
> 技术基线:React 19 + Tailwind CSS 4 + shadcn 风格组件(Radix)。

---

## 目录

- [一、总体评价](#一总体评价)
- [二、P0 高优先级问题与建议](#二p0-高优先级问题与建议)
- [三、P1 中优先级问题与建议](#三p1-中优先级问题与建议)
- [四、P2 低优先级打磨项](#四p2-低优先级打磨项)
- [五、设计系统补充规范建议](#五设计系统补充规范建议)
- [六、值得保持的亮点](#六值得保持的亮点)
- [七、落地路线图](#七落地路线图)
- [附录:走查页面清单](#附录走查页面清单)

---

## 一、总体评价

**基础素质良好,问题集中在"语义一致性、信息密度、可供性误导"三个层面,而不是美观度。**

做得好的部分:

- 设计 token 体系完整(`src/index.css`),亮/暗双主题在所有页面均成立,暗色下卡片与边框层级清晰;
- 圆角(0.75rem 基准)、间距节奏、卡片阴影体系全局统一;
- 页面切换动效(fade + slide-in 300ms)克制得体;
- 细节意识好:`tabular-nums` 数字等宽、`motion-safe` 尊重减少动效偏好、全局 focus-visible ring 兜底。

需要提升的方向:红色语义滥用、表单弹窗可用性缺陷、空页面信息密度不足、图表可读性失真、以及若干"看起来能点但其实不能点"的可供性问题。以下按优先级展开。

---

## 二、P0 高优先级问题与建议

### P0-1 红色(destructive)被滥用,削弱"危险"语义

**现状**

红色在界面中同时承担了 4 种互不相关的含义:

| 场景 | 位置 | 当前用色 |
| --- | --- | --- |
| "未关联简历"标签 | 岗位看板卡片、岗位表格 | destructive 红描边胶囊 |
| "待复习"徽章 | 题库页题目卡片 | 实心红 |
| "已标记没答好" | 面试详情页问题卡片 | 实心红块 |
| "删除"按钮/图标 | 公司卡片、简历卡片、岗位表格行、面试详情页右上角(实心红按钮) | 常驻可见红色 |

**问题**

红色是整个界面视觉权重最高的颜色。当"数据缺失""待办提醒""普通删除"都用红色表达时,真正不可逆的危险操作(设置页"清空所有数据")反而失去辨识度;用户对红色的警觉被钝化,误触风险上升。

**建议**

1. 划清三层语义,写进设计 token 注释:
   - `destructive`(红):仅用于**不可逆/危险**操作与真实错误态;
   - `warning`(琥珀):用于"缺失、待办、需要注意"类状态(未关联简历、待复习、没答好);
   - `muted`(灰):中性占位。
2. 删除交互统一收敛为:**ghost 图标(hover 时变红)+ ConfirmDialog 二次确认**;列表行内不再常驻红色。面试详情页右上角的实心红"删除"应降级为 outline/ghost。
3. 保留设置页"危险操作"区的实心红按钮——那是红色唯一应该以实心出现的地方。

**涉及文件**:`src/components/jobs/JobKanban.tsx`、`src/components/jobs/JobTable.tsx`、`src/pages/KnowledgePage.tsx`、`src/pages/InterviewDetailPage.tsx`、`src/pages/CompaniesPage.tsx`、`src/pages/ResumesPage.tsx`

---

### P0-2 表单弹窗提交按钮会被滚走

**现状**

`src/components/jobs/JobFormDialog.tsx:178`:

```tsx
<DialogContent className="max-h-[90vh] overflow-y-auto sm:max-w-lg">
```

整个弹窗(含标题、表单、底部 DialogFooter)一起滚动。新增岗位表单字段很多(公司/岗位/薪资/地点/简历/类型/状态/优先级/来源/日期/标签/描述/JD),用户填到底部字段时,保存/取消按钮在视口外,既看不到提交入口,也感知不到还剩多少字段。

**建议**

把滚动收窄到表单体,标题与底部按钮 sticky:

```tsx
<DialogContent className="flex max-h-[90vh] flex-col gap-0 overflow-hidden sm:max-w-lg">
  <DialogHeader className="shrink-0 border-b px-6 py-4">…</DialogHeader>
  <div className="flex-1 overflow-y-auto px-6 py-4">
    {/* 全部表单字段 */}
  </div>
  <DialogFooter className="shrink-0 border-t bg-muted/30 px-6 py-3">…</DialogFooter>
</DialogContent>
```

**推广**:该模式应抽成规范,同步应用到 `InterviewFormDialog`、`QuestionFormDialog`、`ResumeVersionFormDialog`、`StartMockDialog` 等所有长表单弹窗。

---

### P0-3 页面下半屏大面积空白,信息密度不足

**现状**

- **复盘页**(`/review`):仅顶部一张待复盘卡片,下方整屏空白,无任何引导或次级内容;
- **面试列表页**(`/interviews`):2 条记录后直接留白到底;
- **总览页**:右栏"即将到来的面试"空态之后,学习重点卡片下方仍有大片空白;
- **面试详情页**在进场动画期间可捕获到整页白帧(动画期间内容未绘制),感知上像"闪白"。

**问题**

这是一个以"复盘 → 复习 → 沉淀"为卖点闭环的产品,但复盘页恰恰是最空的页面。空 ≠ 简洁,空在工具类产品里会被解读为"功能不完整"。

**建议**

按"页面 = 一个工作台"的原则补齐内容结构:

1. **复盘页**:增加"艾宾浩斯复习队列"(今日待复习/3 天/7 天/21 天分层)、"近期复盘完成度"统计条、空态时给出"去题库看看 / 查看日历"的引导卡;
2. **面试列表页**:底部补充"最近添加的题目""临近面试提醒"等次级模块,或至少使用统一的区域级空态组件;
3. **总览页**:右栏允许"即将到来的面试"与"学习重点"两卡自然堆叠后,剩余空间用"本周回顾"摘要填充;
4. **详情页闪白**:页面切换动画(`AppShell.tsx:53` 的 `animate-in`)期间确认骨架/底色一致,避免白帧感,可将动画时长缩短到 200ms 并降低位移幅度。

---

### P0-4 统计卡的 hover 动效是"假可供性"(misleading affordance)

**现状**

`src/components/common/StatCard.tsx:53`:

```tsx
'group relative overflow-hidden transition-all duration-200 hover:-translate-y-0.5 hover:shadow-md active:scale-[0.99]'
```

总览页 4 张统计卡(已投递/面试中/Offer/已结束,`src/pages/DashboardPage.tsx:80-83`)有完整的悬浮位移 + 渐变光晕 + 图标缩放动效,但**组件不接受链接、未绑定任何跳转**。

**问题**

在 Web 心智中,"悬浮浮起 + 阴影"等价于"可点击"。用户尝试点击后无响应,产生挫败感;同时这些动效浪费在了没有行为的元素上。

**建议**(二选一,推荐 A):

- **方案 A**:让统计卡可点击——传入 `to` 属性,内部用 `Link` 包裹,跳转到对应筛选页(已投递 → `/jobs?status=applied`,面试中 → `/interviews?status=pending`…),保留动效;
- **方案 B**:去掉位移与光晕,只保留极轻的边框色变化,回归纯展示卡片。

---

### P0-5 Inter 字体从 Google Fonts 加载,与"本地优先"定位冲突

**现状**

`index.html`:

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
```

**问题**

1. 样式表是渲染阻塞资源,国内网络下可达 1–3 秒白屏或字体闪换(FOUT);
2. 与产品"纯本地、无后端、零外联"的核心卖点直接矛盾(README 明确主打 Local-First);
3. Inter 实际只作用于数字与拉丁字符,中文仍走 PingFang/微软雅黑,收益有限。

**建议**(按优先级):

1. **自托管**:`pnpm add @fontsource-variable/inter`,在 `main.tsx` 引入,删除三个外联标签;
2. 或**直接移除 webfont**:界面数字已有 `tabular-nums` 保证对齐,系统字体栈(`"PingFang SC", "Microsoft YaHei", system-ui`)观感差异极小,却换来零外联、零阻塞。

---

## 三、P1 中优先级问题与建议

### P1-1 同类控件三种样式,缺统一的 SegmentedControl

**现状**

"视图/模式切换"这一动作在三个页面呈现三种形态:

| 页面 | 控件形态 |
| --- | --- |
| 岗位页(看板/表格) | 胶囊 Tabs(Radix Tabs,浅灰底) |
| 分析页(复盘统计/薄弱项分析) | 同款胶囊 Tabs |
| 日历页(月/周/日) | 纯文字 ghost 按钮组,无底色容器 |

另有:总览页 TopNav 的"新增"主按钮与岗位页头部"新增岗位"主按钮同屏出现两个同权重紫色实心按钮。

**建议**

1. 新建 `SegmentedControl` 组件(浅灰容器 + 白色浮起选中项 + 滑动指示),日历页月/周/日接入;
2. 约定层级:**全局主按钮每屏最多一个**。页面头部主按钮保留(它是当前页面的主任务),TopNav"新增"下拉可降为 outline 变体,或保留实心但页面头部改用 outline。

**涉及文件**:`src/pages/CalendarPage.tsx`、`src/pages/JobsPage.tsx`、`src/pages/InsightsPage.tsx`、`src/components/layout/TopNav.tsx`

### P1-2 中英文混排的层级语言不统一

**现状**

- 统计卡 label 因 `uppercase`(`src/components/common/StatCard.tsx:61`)渲染为 **"OFFER"**,与相邻"已投递/面试中/已结束"的中文标签气质冲突;
- 侧边栏分组标题"求职管理"等使用 `uppercase tracking-wider`(`src/components/layout/Sidebar.tsx:66`)——uppercase 对纯中文无效,实际只起作用的字距,样式与意图不符。

**建议**

1. 约定:**中文界面中,英文缩写保持首字母大写原样**(Offer、JD、HR、RAG),层级表达靠字号与颜色,不靠大小写变换;
2. `StatCard` 的 label 移除 `uppercase`,改为 `text-xs font-medium tracking-normal`;代码中 label 统一写作 "Offer";
3. 侧边栏分组标题移除 `uppercase`,保留 `text-[10px]→text-xs` + 中灰即可(小字号问题见 P2-1)。

### P1-3 图表可读性失真

**现状(分析页 `/insights`)**

1. **"最容易被问(标签 Top 10)"** 横轴为 0–1 的比率,刻度 0.25/0.5/0.75/1;标签次数为 1 时所有条形全部顶满——六个标签"一样热",信息失真;
2. **"求职 facile 漏斗"**(总览页)条形无端点数值标签,0 值阶段只剩孤零零的阶段名,需要读者自行去 x 轴对齐估值;
3. **"得分趋势"** 仅 1 个数据点时渲染一颗孤点,观感接近 bug;
4. 漏斗各阶段按 `chart-1..5` 顺序取色,纯装饰性;与状态徽章颜色(笔试蓝点、一面紫点)无对应关系,用户会试图从颜色中读出含义而读取失败。

**建议**

1. 频次类条形图改用**次数作横轴(整数刻度)**,比率只用于"失分率"这类真正的百分比场景;
2. 所有横向条形图(bar 端点)与漏斗添加**数值标签**,弱化或删除 x 轴网格线;
3. 折线图数据点 < 2 时**降级为统计摘要**(平均分 + 最近一次得分大数字展示),不渲染折线;
4. 落地"状态 → 颜色"映射规范(见第五章),图表配色从装饰转向语义。

**涉及文件**:`src/pages/InsightsPage.tsx`、`src/pages/DashboardPage.tsx`、`src/lib/chart-colors.ts`

### P1-4 设置页布局:窄列左置,右侧大面积空白

**现状**

设置页内容约 640px 窄列靠左摆放,右侧留白近一半;且 7 个分区(个人/隐私/分类/提醒/LLM/Sidecar/数据管理)一口气滚到底,查找成本高。

**建议**

两种方向(推荐 B):

- 方案 A:内容列 `mx-auto` 居中,`max-w-2xl`,简单止血;
- 方案 B:经典双栏设置布局——左侧锚点子导航(个人设置 / 隐私模式 / 题库分类 / 面试提醒 / 模型配置 / 数据管理),右侧对应分区,滚动时高亮当前分区;分区卡片保持现有样式不动,改动成本可控。

### P1-5 看板横向滚动无暗示 + 模拟页等高空卡的空白

**现状**

- 岗位看板共 8 个阶段列,1440px 视口下必然截断,但没有渐隐遮罩、局部阴影等"右侧还有内容"的暗示,新用户可能只看到 4 列;
- 模拟面试页"我的简历"卡片为与右侧"我的项目"等高,内容只有 3 行,下方大片空白。

**建议**

1. 看板滚动容器右缘加 24–32px 渐隐遮罩(白色→透明,暗色主题同步),并在 scrollLeft > 0 时左缘同样处理;
2. 等高策略改为:卡片高度自适应内容,grid 用 `items-start`;左卡空余处可补充"简历版本切换快捷入口"或直接两卡不等高。

---

## 四、P2 低优先级打磨项

### P2-1 中文小字号低于可读下限

侧边栏分组标题 `text-[10px]`(`Sidebar.tsx:66`)、品牌副标题 `text-[11px]`(`Sidebar.tsx:60`)。CJK 字形在 12px 以下笔画粘连明显。**建议**:中文小字一律 ≥12px;层级改用颜色(如 `text-muted-foreground`)与字重区分。

### P2-2 搜索框 placeholder 对比度不足

TopNav 搜索按钮 placeholder 用 `text-muted-foreground/70`(`TopNav.tsx:157`),约 3.8:1,低于 WCAG AA 的 4.5:1。**建议**:placeholder/提示类文字最低用 `text-muted-foreground` 全值(#71717a,4.7:1)。

### P2-3 题库左栏全零分类常驻

"大模型基础 0 / SFT 0 / Agent 0" 长期占据导航位。**建议**:计数为 0 的分类折叠到"其他"或沉底,hover 展开即可。

### P2-4 表格操作列常驻三个图标

岗位表格每行常驻"关联/编辑/删除"图标,红色删除图标永远可见。**建议**:操作图标默认透明(`opacity-0`),行 hover 时浮现(`group-hover:opacity-100`),配合已有的行 hover 底色(`src/components/ui/table.tsx:21`)形成干净的静默态;键盘可达性用 focus-within 兜底。

### P2-5 区域级空态未统一

`EmptyState` 组件(图标 + 标题 + 描述 + 动作按钮,`src/components/common/EmptyState.tsx`)质量不错,但"暂无待面试安排"(总览)、"暂无足够数据"(分析页图表)等区域级空态仍是裸文本。**建议**:小型空态变体(`EmptyState size="sm"`)统一接入。

### P2-6 列表页无加载骨架

目前仅 hydration 阶段有骨架屏(`AppShell.tsx:29-39`)。本地数据快,感知影响小,但作为设计系统完整性建议补 Skeleton 变体:列表行骨架(面试/题库)、看板列骨架。

---

## 五、设计系统补充规范建议

以下规范建议沉淀到代码注释或 `CLAUDE.md`/团队文档中,防止后续开发漂移:

### 5.1 状态 → 颜色映射表(全局唯一)

| 状态 | 颜色 token | 备注 |
| --- | --- | --- |
| 已投递 | `info`(蓝) | 流程起点 |
| 笔试 | `info` 变体或 chart-2 | 与徽章点颜色一致 |
| 一面/二面/HR面 | `primary`(紫) | 进行中 |
| 已获Offer / 接受Offer | `success`(绿) | 正向结果 |
| 已结束 | `muted`(灰) | 终态 |
| 待面试/待复盘/待复习 | `warning`(琥珀) | 需要注意,不用红 |
| 未关联简历/数据缺失 | `muted` 或 warning | 中性提醒,不用红 |
| 错误/不可逆操作 | `destructive`(红) | 唯一红色场景 |

原则:**同一个状态在徽章、看板列、图表、日历 chip 中必须同色**。当前漏斗取色(chart-1..5 顺序)与徽章取色是两套体系,应统一到上表。

### 5.2 组件规范速查

- **主按钮**:每屏最多 1 个实心 `primary`;页面头部与全局 TopNav 不同时使用实心;
- **弹窗**:标题/底栏 sticky,仅表单体滚动;底栏带 `border-t`;
- **删除**:ghost 图标 + hover 变红 + 二次确认;实心红仅限不可逆数据操作;
- **SegmentedControl**:所有"视图切换"统一控件;
- **中文排版**:正文 ≥14px,辅助 ≥12px;不使用 uppercase 表达层级;英文缩写首字母大写原样;
- **空态**:统一 `EmptyState`(含 sm 变体),文案遵循"说明 + 下一步动作"结构;
- **动效**:悬浮位移/阴影仅用于可点击元素;时长 150–200ms;无限循环动画必须 `motion-safe` 包裹(现有 imminent-glow 已是正确示范)。

### 5.3 可新增的 token

```css
--warning-foreground 组已存在,补充使用规范即可;
可考虑:--shadow-card(统一卡片阴影)、--shadow-pop(浮层阴影)层级化。
```

---

## 六、值得保持的亮点

1. **Token 化主题体系**:亮/暗双主题全页面成立,暗色下边框/卡片层级清晰(`src/index.css`);
2. **细节意识**:`tabular-nums` 统计数字等宽、`motion-safe` 尊重系统动效偏好(imminent-glow 呼吸光晕)、全局 `focus-visible` ring;
3. **情感化设计**:临近面试的呼吸光晕动画是低成本高感知的好点子;
4. **轻量信息组件**:公司首字彩色头像、简历 v1/v2 版本胶囊、搜索框 kbd 快捷键提示、状态点徽章;
5. **基础组件质量**:`EmptyState`、`StatCard` 的 tone 体系、表格行 hover 底色,都是可以直接扩展的底子。

---

## 七、落地路线图

| 阶段 | 内容 | 预估工作量 | 收益 |
| --- | --- | --- | --- |
| **第一阶段(1 天)** | P0-5 字体自托管/移除外联;P0-2 弹窗 sticky footer;P2-2 对比度;P2-1 中文最小字号 | ~1 天 | 感知最直接,消除"卡/慢"与填表挫败 |
| **第二阶段(2–3 天)** | P0-1 红色语义收敛 + warning 用法;P0-4 统计卡可点击;P1-1 SegmentedControl 统一;P2-4 表格操作 hover 浮现 | 2–3 天 | 视觉专业感核心提升 |
| **第三阶段(3–5 天)** | P0-3 复盘页/面试页/总览内容密度重构;P1-4 设置页双栏;P1-5 看板滚动暗示与等高策略 | 3–5 天 | 页面从"列表"升级为"工作台" |
| **第四阶段(2 天)** | P1-2 中英文层级规范;P1-3 图表数值轴/标签/降级策略;P2-3/P2-5/P2-6 打磨 | ~2 天 | 长期一致性 |
| **持续** | 第五章设计规范沉淀进团队文档/CLAUDE.md,新功能按规范走查 | — | 防漂移 |

---

## 附录:走查页面清单

| # | 路由 | 视图 | 主题 | 备注 |
| --- | --- | --- | --- | --- |
| 1 | `/` | 总览 | 亮/暗 | 统计卡、漏斗、学习重点 |
| 2 | `/jobs` | 岗位看板 | 亮/暗 | 8 列截断、未关联简历标签 |
| 3 | `/jobs` | 岗位表格 | 亮 | 操作列、状态徽章 |
| 4 | `/jobs/j1` | 岗位详情 | 亮 | JD 折叠、面试时间线 |
| 5 | `/interviews` | 面试列表 | 亮 | 空白密度问题 |
| 6 | `/interviews/i2` | 面试详情 | 亮 | 实心红删除、题目卡片 |
| 7 | `/mock` | 模拟面试 | 亮 | 等高空卡、警告横幅 |
| 8 | `/calendar` | 日历(月) | 亮 | 月/周/日控件样式 |
| 9 | `/review` | 复盘 | 亮 | 大面积空白 |
| 10 | `/knowledge` | 题库 | 亮 | 待复习红徽章、全零分类 |
| 11 | `/insights` | 分析 | 亮 | 比率轴、孤点趋势图 |
| 12 | `/companies` | 公司 | 亮 | 卡片操作、头像 |
| 13 | `/resumes` | 简历 | 亮 | 版本胶囊 |
| 14 | `/settings` | 设置 | 亮 | 窄列布局、危险操作区 |
| 15 | `/jobs?new=1` | 新增岗位弹窗 | 亮 | 底部按钮滚出视口 |
| 16 | `/`(390×844) | 移动端结构 | — | DOM 验证:汉堡菜单/搜索图标正常降级 |

> 说明:模拟面试会话页(`/mock/:id`)需配置 LLM API 后才有内容,本次未做视觉走查;移动端视口下截图通道受限,以 DOM 快照验证结构,建议后续人工补一轮真机视觉走查。
